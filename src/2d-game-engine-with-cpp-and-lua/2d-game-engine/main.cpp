#define WIN32_LEAN_AND_MEAN
#include <Windows.h>
import std;

auto wWinMain(
    HINSTANCE hInstance,
    HINSTANCE hPrevInstance,
    LPWSTR lpCmdLine,
    int nCmdShow
) -> int
{
    return 0;
}
